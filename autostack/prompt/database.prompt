## 任务
根据我的需求文档和数据库设计文档为我生成prisma实体类.

### 需求文档：
```
${requirement_doc}
```

### 数据库设计文档
```
${database_design_doc}
```
### 已有的prisma内容
```
${prisma_schema}
```

## 要求:
1. 根据需求文档和数据库设计文档生成prisma实体类.
2. 只输出prisma实体类,不要输出其他任何内容.
3. 如果已有的prisma内容中已经包含了需要的实体类, 那么可以在原有的基础上进行添加.
4. 不要删除已有的实体类的属性.
