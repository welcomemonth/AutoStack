## 任务
根据我的需求文档和postgresql数据库设计文档为我生成prisma实体类.

### 需求文档：
```
${requirement_doc}
```

### 数据库设计文档
```
${database_design_doc}
```

### 已有的prisma内容
```
${prisma_schema}
```

## 要求：
1. **生成 Prisma 实体类**：根据需求文档和数据库设计文档为我生成 Prisma 实体类。
2. **确保关系模型的正确性**：在生成关系时，请遵循以下规则：
   - 在 **关系字段** 中不要直接指定 `fields` 和 `references`，这些应该仅在 **关联的另一端模型** 中指定。
   - 请使用 **`@relation`** 属性指定关系名称而不是字段。
   - 对于一对多关系，请确保多方定义正确的关系字段数组。
   - 对于多对多关系，可以通过中介模型（如 `Follow`）进行处理，确保该模型定义正确。
3. **不修改已有实体类的属性**：如果已有的实体类包含属性，请保留它们，不进行修改。
4. **避免不必要的属性或字段重复**：确保字段定义清晰且没有冗余。
5. **时间字段处理**：所有时间字段（如 `create_time` 和 `update_time`）请保持一致的格式和定义方式，确保正确设置默认值（如 `@default(now())`）。
6. **保持可扩展性和清晰的命名**：生成的模型应该清晰且具备可扩展性，命名应符合领域语义。
7. **输出格式要求**：仅输出 Prisma schema 文件内容，不要输出其他信息。
8. **模型与数据库一致性**：请确保生成的 Prisma 模型与数据库设计文档保持一致，能够正确映射数据库表和字段。

